# MVP 完备化 M4-C 个人资料编辑 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 个人资料编辑——编辑屏可改 username（处理 unique 冲突）+ 上传头像（expo-image-picker → Storage avatars bucket）+ 个人简介 bio；个人页接真头像与 bio。

**Architecture:** profiles 表加 bio 列 + 新建 avatars Storage bucket；profilesRepo 提供读/更新/头像上传；新增 app/profile/edit.tsx 编辑屏，个人页加编辑入口并用 UserAvatar 显示真 avatar_url + 显示 bio；改名后同步 auth store username。

**Tech Stack:** Expo SDK 54 / RN 0.81 / TS、Supabase（Postgres + Storage）、expo-image-picker、@tanstack/react-query、vitest。

## Global Constraints

- 设计 spec：`docs/superpowers/specs/2026-06-26-mvp-completion-design.md`（M4-C 节为准）。
- 本计划只做 M4-C，是 MVP 完备化最后一个子里程碑。
- 保留 hasSupabase=false 本地保底：未配 Supabase 时编辑入口可禁用/隐藏，不崩。
- username 允许改名，撞 unique 提示「用户名已被占用」；复用现有 validateUsername。
- 头像上传失败不阻断其他字段保存；图片压缩控制大小；bio ≤80 字。
- 复用 M4-A 的 UserAvatar 组件。
- 提交信息末尾带 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 不写任何密钥；不改 .env / .env.example。
- migration SQL + Storage bucket 由人工在 Supabase 后台执行；plan 只写文件。
- 网络/屏幕代码不写 vitest 单测（靠 typecheck）；纯函数（如 bio 校验）写单测。

---

## Task 1: profiles 加 bio 列 + avatars bucket（migration）

**Files:**
- Create: `supabase/migrations/0011_profile_bio_avatars.sql`

**Interfaces:**
- Produces: `profiles.bio` 列；Storage bucket `avatars`（公开读 + 只能写/改自己目录）。

> 说明：SQL，人工后台执行。

- [ ] **Step 1: 写 migration**

Create `supabase/migrations/0011_profile_bio_avatars.sql`:

```sql
-- 0011_profile_bio_avatars.sql
-- 资料编辑：profiles 加 bio 列 + avatars Storage bucket。

alter table public.profiles add column if not exists bio text;

insert into storage.buckets (id, name, public)
values ('avatars', 'avatars', true) on conflict do nothing;

-- 公开读
create policy "avatars_public_read" on storage.objects
  for select using (bucket_id = 'avatars');

-- 只能写/改/删自己目录({uid}/...)下的文件：路径第一段必须等于 auth.uid()
create policy "avatars_insert_own" on storage.objects
  for insert with check (
    bucket_id = 'avatars' and (storage.foldername(name))[1] = auth.uid()::text
  );
create policy "avatars_update_own" on storage.objects
  for update using (
    bucket_id = 'avatars' and (storage.foldername(name))[1] = auth.uid()::text
  );
```

- [ ] **Step 2: 提交（人工后台执行 SQL）**

```bash
git add supabase/migrations/0011_profile_bio_avatars.sql
git commit -m "feat(db): profiles 加 bio 列 + avatars Storage bucket (M4-C)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: 安装 expo-image-picker + 配权限

**Files:**
- Modify: `package.json`、`package-lock.json`、`app.json`

**Interfaces:**
- Produces: expo-image-picker 可用；app.json 配相册权限说明。

> 说明：装原生模块。dev build 需重新 `expo run:ios` 才生效（人工，最后验证时做）。

- [ ] **Step 1: 安装**

Run: `npx expo install expo-image-picker`
Expected: 装上与 SDK 54 匹配的版本。

- [ ] **Step 2: app.json 配权限**

读 `app.json`，在 `expo.plugins` 数组加 expo-image-picker 插件配置（含相册权限说明文案）：

```json
[
  "expo-image-picker",
  {
    "photosPermission": "允许 AI Shorts 访问相册以上传头像。"
  }
]
```

（加到现有 plugins 数组里，保留其它插件。）

- [ ] **Step 3: typecheck**

Run: `npm run typecheck`
Expected: 无错误。

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json app.json
git commit -m "chore: 装 expo-image-picker + 配相册权限（头像上传）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: bio 校验纯函数 + 单测

**Files:**
- Modify: `src/api/auth/validateUsername.ts`（追加 validateBio）
- Modify: `src/api/auth/validateUsername.test.ts`（追加 bio 测试）

**Interfaces:**
- Produces: `validateBio(raw: string): { ok: boolean; msg?: string }`（≤80 字，允许空）。

- [ ] **Step 1: 追加失败测试**

在 `src/api/auth/validateUsername.test.ts` 追加：

```ts
import { validateBio } from './validateUsername';

describe('validateBio', () => {
  it('accepts empty', () => expect(validateBio('').ok).toBe(true));
  it('accepts under 80', () => expect(validateBio('a'.repeat(80)).ok).toBe(true));
  it('rejects over 80', () => expect(validateBio('a'.repeat(81)).ok).toBe(false));
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm run test:run -- src/api/auth/validateUsername.test.ts`
Expected: FAIL（validateBio is not a function / not exported）

- [ ] **Step 3: 写实现**

在 `src/api/auth/validateUsername.ts` 追加：

```ts
export function validateBio(raw: string): Validation {
  if (raw.length > 80) return { ok: false, msg: '简介最多 80 个字符' };
  return { ok: true };
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm run test:run -- src/api/auth/validateUsername.test.ts`
Expected: PASS

- [ ] **Step 5: typecheck + Commit**

```bash
npm run typecheck
git add src/api/auth/validateUsername.ts src/api/auth/validateUsername.test.ts
git commit -m "feat(auth): validateBio 纯函数 + 单测

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: profilesRepo（读/更新/头像上传）

**Files:**
- Create: `src/api/supabase/profilesRepo.ts`

**Interfaces:**
- Consumes: `supabase()`、`useAuth`、ProfileRow。
- Produces:
  - `getProfile(userId): Promise<{ id; username; avatar_url; bio } | null>`
  - `updateProfile(input: { username?: string; avatarUrl?: string; bio?: string }): Promise<void>`（更新当前用户；username 撞 unique 抛带标识错误）
  - `uploadAvatar(localUri: string): Promise<string>`（上传 Storage 返回 publicUrl）

> 说明：网络仓库，无单测，靠 typecheck。先读 src/api/client.ts、src/store/auth.ts、src/api/supabase/rows.ts(ProfileRow)。

- [ ] **Step 1: 写 profilesRepo**

Create `src/api/supabase/profilesRepo.ts`:

```ts
import { supabase } from '@/api/client';
import { useAuth } from '@/store/auth';

export interface ProfileData {
  id: string;
  username: string;
  avatar_url: string | null;
  bio: string | null;
}

export class UsernameTakenError extends Error {
  code = 'username_taken' as const;
  constructor() { super('用户名已被占用'); this.name = 'UsernameTakenError'; }
}

export async function getProfile(userId: string): Promise<ProfileData | null> {
  const { data, error } = await supabase()
    .from('profiles').select('id,username,avatar_url,bio').eq('id', userId).maybeSingle();
  if (error) throw error;
  return (data as ProfileData | null) ?? null;
}

export async function updateProfile(input: { username?: string; avatarUrl?: string; bio?: string }): Promise<void> {
  const uid = useAuth.getState().user?.id;
  if (!uid) throw new Error('未登录');
  const patch: Record<string, unknown> = {};
  if (input.username !== undefined) patch.username = input.username;
  if (input.avatarUrl !== undefined) patch.avatar_url = input.avatarUrl;
  if (input.bio !== undefined) patch.bio = input.bio;
  const { error } = await supabase().from('profiles').update(patch).eq('id', uid);
  if (error) {
    // Postgres unique violation code 23505
    if ((error as any).code === '23505') throw new UsernameTakenError();
    throw error;
  }
}

// 上传头像到 avatars/{uid}/avatar.jpg，返回带 cache-buster 的 publicUrl
export async function uploadAvatar(localUri: string): Promise<string> {
  const uid = useAuth.getState().user?.id;
  if (!uid) throw new Error('未登录');
  const res = await fetch(localUri);
  const blob = await res.arrayBuffer();
  const path = `${uid}/avatar.jpg`;
  const { error } = await supabase().storage.from('avatars')
    .upload(path, new Uint8Array(blob), { contentType: 'image/jpeg', upsert: true });
  if (error) throw error;
  const { data } = supabase().storage.from('avatars').getPublicUrl(path);
  // upsert 覆盖同名文件，加 cache-buster 让客户端拉新图
  return `${data.publicUrl}?t=${Date.now()}`;
}
```

> 注意：`Date.now()` 在 RN 运行时可用（vitest 沙箱才禁用，这是运行时代码不是 workflow 脚本）。若需要也可用别的 cache-buster。

- [ ] **Step 2: typecheck + Commit**

```bash
npm run typecheck
git add src/api/supabase/profilesRepo.ts
git commit -m "feat(api): profilesRepo（读/更新/头像上传，username 冲突识别）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 编辑资料屏 app/profile/edit.tsx

**Files:**
- Create: `app/profile/edit.tsx`
- Modify: `app/_layout.tsx`（注册 profile/edit 路由，modal 呈现）

**Interfaces:**
- Consumes: profilesRepo（getProfile/updateProfile/uploadAvatar）、validateUsername/validateBio、UserAvatar、useAuth、expo-image-picker。

> 说明：先读 app/_layout.tsx 看 Stack.Screen 注册方式 + app/video/[id].tsx 等现有 modal 屏的结构与样式风格。

- [ ] **Step 1: 写编辑屏**

Create `app/profile/edit.tsx`：modal 屏，包含：
- 顶部 header：返回 X + 标题「编辑资料」+ 保存按钮
- 头像区：`<UserAvatar user={{ username, avatar_url: avatarUrl }} size={88} />` + 「更换头像」按钮 → `ImagePicker.launchImageLibraryAsync({ mediaTypes: ['images'], allowsEditing: true, aspect: [1,1], quality: 0.6 })` → 选中后 setAvatarLocalUri 预览
- username 输入框（用 validateUsername 实时校验，显示错误）
- bio 多行输入框（用 validateBio，显示 N/80 计数）
- 进屏 useQuery getProfile(user.id) 预填当前值
- 保存逻辑：
  1. 校验 username/bio，不过则提示不保存
  2. 若选了新头像 localUri：先 await uploadAvatar(localUri) 拿 publicUrl（失败则 toast「头像上传失败」但继续保存其它字段，按 spec「头像上传失败不阻断」）
  3. await updateProfile({ username, bio, avatarUrl? })；撞 UsernameTakenError → 提示「用户名已被占用」，停在本屏
  4. 成功：invalidate ['myVideos']、['profile', uid]；同步 auth store username（useAuth.setState 或提供的方法）；toast「已保存」；router.back()
- 提交中 disable 保存按钮

关键片段（保存）：

```tsx
const onSave = async () => {
  const uv = validateUsername(username);
  const bv = validateBio(bio);
  if (!uv.ok) { setErr(uv.msg!); return; }
  if (!bv.ok) { setErr(bv.msg!); return; }
  setSaving(true);
  try {
    let newAvatarUrl: string | undefined;
    if (avatarLocalUri) {
      try { newAvatarUrl = await uploadAvatar(avatarLocalUri); }
      catch { showToast({ message: '头像上传失败，其它信息已保存' }); }
    }
    await updateProfile({ username: username.trim(), bio: bio.trim(), avatarUrl: newAvatarUrl });
    // 同步 auth store 的 username
    useAuth.setState({ user: { ...(useAuth.getState().user!), username: username.trim() } });
    qc.invalidateQueries({ queryKey: ['profile', uid] });
    qc.invalidateQueries({ queryKey: ['myVideos', uid] });
    showToast({ message: '已保存' });
    router.back();
  } catch (e: any) {
    if (e?.code === 'username_taken') setErr('用户名已被占用');
    else setErr(e?.message ?? '保存失败');
  } finally { setSaving(false); }
};
```

（`useAuth.setState` 若 store 未暴露，改用 store 提供的 setter；先读 auth.ts 确认。`uid` 来自 useAuth。）

- [ ] **Step 2: 注册路由**

`app/_layout.tsx` 的 Stack 加：`<Stack.Screen name="profile/edit" options={{ presentation: 'modal' }} />`（参考现有 modal 屏注册）。

- [ ] **Step 3: typecheck**

Run: `npm run typecheck`
Expected: 无错误。

- [ ] **Step 4: Commit**

```bash
git add app/profile/edit.tsx app/_layout.tsx
git commit -m "feat(profile): 编辑资料屏（username/头像/bio）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: 个人页接入编辑入口 + 真头像 + bio

**Files:**
- Modify: `app/(tabs)/profile.tsx`

**Interfaces:**
- Consumes: profilesRepo.getProfile、UserAvatar、router。

> 说明：个人页现在头像传 avatar_url:null、无 bio、无编辑入口。改为查 getProfile 拿真 avatar_url/bio。

- [ ] **Step 1: profile 接 getProfile + 编辑入口**

`app/(tabs)/profile.tsx`：
- useQuery getProfile(user.id)（queryKey ['profile', user.id]，enabled: hasSupabase && !!user）。
- 头像 `<UserAvatar user={{ username: user?.username, avatar_url: profile?.avatar_url ?? null }} size={80} />`。
- 在用户名下方显示 bio（profile?.bio，有才显示）。
- 加「编辑资料」按钮（在现有「发布新作品」附近或头部），onPress `router.push('/profile/edit')`。仅 hasSupabase && 登录时显示。

- [ ] **Step 2: typecheck + Commit**

```bash
npm run typecheck
git add "app/(tabs)/profile.tsx"
git commit -m "feat(profile): 个人页接真头像/bio + 编辑入口

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: 评论/通知 actor 接真头像

**Files:**
- Modify: `src/api/supabase/mappers.ts`（rowToComment 带 avatar_url）
- Modify: `src/store/comments.ts`（Comment 加 authorAvatarUrl）
- Modify: `src/components/comments/CommentsSheet.tsx`（UserAvatar 传真 avatar_url）

**Interfaces:**
- Consumes: CommentRow.author.avatar_url。

> 说明：M4-A 时 comments 头像传了 null，现在 profiles 有头像了，接上。先读 rowToComment（mappers.ts）、Comment 类型（comments.ts）、CommentsSheet 头像渲染。

- [ ] **Step 1: Comment 加 authorAvatarUrl**

`src/store/comments.ts`：Comment 接口加 `authorAvatarUrl?: string | null`。

- [ ] **Step 2: rowToComment 带 avatar_url**

`src/api/supabase/mappers.ts` 的 rowToComment：加 `authorAvatarUrl: row.author?.avatar_url ?? null`。

- [ ] **Step 3: CommentsSheet 用真头像**

`src/components/comments/CommentsSheet.tsx`：评论项 `<UserAvatar user={{ username: comment.authorName, avatar_url: comment.authorAvatarUrl ?? null }} size={32} />`。

- [ ] **Step 4: typecheck + Commit**

```bash
npm run typecheck
git add src/api/supabase/mappers.ts src/store/comments.ts src/components/comments/CommentsSheet.tsx
git commit -m "feat(profile): 评论 actor 接真头像

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 完成标准（M4-C 验收）
- 进编辑资料屏能预填当前 username/头像/bio。
- 改 username 保存成功，撞已占用名提示「用户名已被占用」。
- 选图上传头像成功，个人页/评论显示新头像。
- 改 bio 保存，个人页显示。
- 头像上传失败不阻断 username/bio 保存。
- 未配 Supabase 不显示编辑入口，不崩。
- `npm run typecheck` 与 `npm run test:run` 全绿。

## 人工操作
- 执行 migration 0011（bio 列 + avatars bucket）。
- 重新 `expo run:ios`（装了 expo-image-picker 原生模块）。
- 端到端验证上述完成标准。

## 后续（不在本计划）
- 通知 inbox 的 actor 头像（notificationMapper 已带 actorAvatarUrl，inbox 渲染时已可用真头像，若未接可顺带）。
- M4 累积 Minor 清单（见 ledger）择优清理。
