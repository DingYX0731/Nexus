# MVP 完备化 M4-B 社交基建 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建社交基建——follows 表（关注关系）+ notifications 表（4 类通知，DB 触发器自动生成）+ 关注/取关 UI + 个人页关注/粉丝数 + inbox 接真数据。

**Architecture:** 新增 follows/notifications 两张表 + RLS；4 个 SECURITY DEFINER 触发器在 likes/comments/videos(续写)/follows 插入时自动写通知（自己对自己的动作不发）；新增 followsRepo/notificationsRepo + mapper；视频详情页与个人页加关注按钮，个人页加关注/粉丝数，inbox 用 listNotifications 替换 mock + 进页面标记已读 + tab 未读红点。

**Tech Stack:** Expo SDK 54 / RN 0.81 / TS、Supabase（Postgres + RLS + 触发器）、@tanstack/react-query、vitest。

## Global Constraints

- 设计 spec：`docs/superpowers/specs/2026-06-26-mvp-completion-design.md`（M4-B 节为准）。
- 本计划只做 M4-B。M4-C（资料编辑）是后续 plan。
- 保留 hasSupabase=false 本地保底：关注/通知是云端能力，未配 Supabase 时关注按钮禁用或隐藏、inbox 显示空态（不崩）。
- 通知由 DB 触发器生成（不靠客户端）；自己对自己的动作（自赞/自评/自续/自关注）不发通知。
- RLS：follows 所有人可读、只能增删自己的（follower_id=auth.uid()）、禁自关注；notifications 只读自己的、客户端不能 insert、可 update 自己的 read。
- 复用 M4-A 组件：UserAvatar、LoadingState/ErrorState/EmptyState。
- 提交信息末尾带 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 不写任何密钥；不改 .env / .env.example。
- migration SQL 由人工在 Supabase 后台执行；plan 只写文件。
- 网络/屏幕代码不写 vitest 单测（靠 typecheck）；纯函数（mapper）写单测。

---

## Task 1: follows 表 + notifications 表 + RLS（migration）

**Files:**
- Create: `supabase/migrations/0009_social.sql`

**Interfaces:**
- Produces: 表 `public.follows(follower_id, followee_id, created_at)`、`public.notifications(id, user_id, actor_id, type, video_id, comment_id, read, created_at)` + RLS + 防自关注。

> 说明：SQL，人工后台执行。触发器在 Task 2 单独建（便于评审分离）。

- [ ] **Step 1: 写 migration**

Create `supabase/migrations/0009_social.sql`:

```sql
-- 0009_social.sql
-- 社交基建：follows（关注关系）+ notifications（通知）+ RLS。

create table public.follows (
  follower_id uuid references public.profiles(id) on delete cascade,
  followee_id uuid references public.profiles(id) on delete cascade,
  created_at  timestamptz not null default now(),
  primary key (follower_id, followee_id),
  constraint no_self_follow check (follower_id <> followee_id)
);

create table public.notifications (
  id         uuid primary key default gen_random_uuid(),
  user_id    uuid not null references public.profiles(id) on delete cascade,  -- 接收者
  actor_id   uuid references public.profiles(id) on delete cascade,            -- 触发者
  type       text not null,         -- like | comment | fork | follow
  video_id   uuid references public.videos(id) on delete cascade,
  comment_id uuid references public.comments(id) on delete cascade,
  read       boolean not null default false,
  created_at timestamptz not null default now()
);

create index follows_follower_idx   on public.follows(follower_id);
create index follows_followee_idx   on public.follows(followee_id);
create index notifications_user_idx on public.notifications(user_id, created_at desc);

alter table public.follows       enable row level security;
alter table public.notifications enable row level security;

-- follows：所有人可读；只能增删自己的
create policy "follows_read_all" on public.follows
  for select using (true);
create policy "follows_insert_own" on public.follows
  for insert with check (follower_id = auth.uid());
create policy "follows_delete_own" on public.follows
  for delete using (follower_id = auth.uid());

-- notifications：只读自己的；客户端不能 insert（只由触发器写）；可 update 自己的(标记已读)
create policy "notifications_read_own" on public.notifications
  for select using (user_id = auth.uid());
create policy "notifications_update_own" on public.notifications
  for update using (user_id = auth.uid());
```

- [ ] **Step 2: 提交（人工后台执行 SQL）**

```bash
git add supabase/migrations/0009_social.sql
git commit -m "feat(db): follows + notifications 表 + RLS (M4-B)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: 通知触发器（migration）

**Files:**
- Create: `supabase/migrations/0010_notification_triggers.sql`

**Interfaces:**
- Produces: 4 个 SECURITY DEFINER 触发器函数 + 触发器：likes/comments/videos(续写)/follows 插入时写 notifications，自己对自己不发。

- [ ] **Step 1: 写 migration**

Create `supabase/migrations/0010_notification_triggers.sql`:

```sql
-- 0010_notification_triggers.sql
-- 通知触发器：在 likes/comments/videos(续写)/follows 插入时自动写 notifications。
-- 自己对自己的动作不发。SECURITY DEFINER 绕过 notifications 的 insert 限制。

-- like：给视频作者发，actor=点赞者
create or replace function public.notify_on_like()
returns trigger language plpgsql security definer set search_path = public as $$
declare v_author uuid;
begin
  select author_id into v_author from public.videos where id = new.video_id;
  if v_author is not null and v_author <> new.user_id then
    insert into public.notifications (user_id, actor_id, type, video_id)
    values (v_author, new.user_id, 'like', new.video_id);
  end if;
  return new;
end; $$;
create trigger on_like_created after insert on public.likes
  for each row execute function public.notify_on_like();

-- comment：给视频作者发，actor=评论者
create or replace function public.notify_on_comment()
returns trigger language plpgsql security definer set search_path = public as $$
declare v_author uuid;
begin
  select author_id into v_author from public.videos where id = new.video_id;
  if v_author is not null and v_author <> new.author_id then
    insert into public.notifications (user_id, actor_id, type, video_id, comment_id)
    values (v_author, new.author_id, 'comment', new.video_id, new.id);
  end if;
  return new;
end; $$;
create trigger on_comment_created after insert on public.comments
  for each row execute function public.notify_on_comment();

-- fork：续写/remix（videos.parent_id 非空）时给父视频作者发，actor=新视频作者
create or replace function public.notify_on_fork()
returns trigger language plpgsql security definer set search_path = public as $$
declare v_parent_author uuid;
begin
  if new.parent_id is not null then
    select author_id into v_parent_author from public.videos where id = new.parent_id;
    if v_parent_author is not null and v_parent_author <> new.author_id then
      insert into public.notifications (user_id, actor_id, type, video_id)
      values (v_parent_author, new.author_id, 'fork', new.id);
    end if;
  end if;
  return new;
end; $$;
create trigger on_fork_created after insert on public.videos
  for each row execute function public.notify_on_fork();

-- follow：给被关注者发，actor=关注者
create or replace function public.notify_on_follow()
returns trigger language plpgsql security definer set search_path = public as $$
begin
  insert into public.notifications (user_id, actor_id, type)
  values (new.followee_id, new.follower_id, 'follow');
  return new;
end; $$;
create trigger on_follow_created after insert on public.follows
  for each row execute function public.notify_on_follow();
```

- [ ] **Step 2: 提交（人工后台执行 SQL）**

```bash
git add supabase/migrations/0010_notification_triggers.sql
git commit -m "feat(db): 4 类通知触发器（like/comment/fork/follow）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: notifications 行类型 + mapper + 单测

**Files:**
- Modify: `src/api/supabase/rows.ts`（加 NotificationRow）
- Create: `src/api/supabase/notificationMapper.ts`
- Create: `src/api/supabase/notificationMapper.test.ts`

**Interfaces:**
- Produces: `NotificationRow` 类型；`rowToNotification(row): NotificationItem`；`NotificationItem` 类型 `{ id, type, actorName, actorAvatarUrl, videoId, createdAt, read }`。

- [ ] **Step 1: 加 NotificationRow 类型**

在 `src/api/supabase/rows.ts` 追加：

```ts
export interface NotificationRow {
  id: string;
  user_id: string;
  actor_id: string | null;
  type: string;            // like | comment | fork | follow
  video_id: string | null;
  comment_id: string | null;
  read: boolean;
  created_at: string;
  actor?: ProfileRow | null;  // join 出的触发者资料
}
```

- [ ] **Step 2: 写失败测试**

Create `src/api/supabase/notificationMapper.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { rowToNotification } from './notificationMapper';
import type { NotificationRow } from './rows';

const base: NotificationRow = {
  id: 'n1', user_id: 'u1', actor_id: 'u2', type: 'like',
  video_id: 'v1', comment_id: null, read: false, created_at: '2026-06-26T00:00:00Z',
  actor: { id: 'u2', username: 'luna', avatar_url: null, created_at: '2026-06-26T00:00:00Z' },
};

describe('rowToNotification', () => {
  it('maps core fields', () => {
    const n = rowToNotification(base);
    expect(n.id).toBe('n1');
    expect(n.type).toBe('like');
    expect(n.actorName).toBe('luna');
    expect(n.videoId).toBe('v1');
    expect(n.read).toBe(false);
  });
  it('handles missing actor', () => {
    const n = rowToNotification({ ...base, actor: null, actor_id: null });
    expect(n.actorName).toBe('已注销用户');
  });
  it('maps created_at to epoch ms', () => {
    expect(rowToNotification(base).createdAt).toBe(new Date(base.created_at).getTime());
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `npm run test:run -- src/api/supabase/notificationMapper.test.ts`
Expected: FAIL（Cannot find module './notificationMapper'）

- [ ] **Step 4: 写实现**

Create `src/api/supabase/notificationMapper.ts`:

```ts
import type { NotificationRow } from './rows';

export interface NotificationItem {
  id: string;
  type: string;            // like | comment | fork | follow
  actorName: string;
  actorAvatarUrl: string | null;
  videoId: string | null;
  createdAt: number;
  read: boolean;
}

export function rowToNotification(row: NotificationRow): NotificationItem {
  return {
    id: row.id,
    type: row.type,
    actorName: row.actor?.username ?? '已注销用户',
    actorAvatarUrl: row.actor?.avatar_url ?? null,
    videoId: row.video_id,
    createdAt: new Date(row.created_at).getTime(),
    read: row.read,
  };
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `npm run test:run -- src/api/supabase/notificationMapper.test.ts`
Expected: PASS

- [ ] **Step 6: typecheck + Commit**

```bash
npm run typecheck
git add src/api/supabase/rows.ts src/api/supabase/notificationMapper.ts src/api/supabase/notificationMapper.test.ts
git commit -m "feat(api): NotificationRow + rowToNotification + 单测

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: followsRepo + notificationsRepo

**Files:**
- Create: `src/api/supabase/followsRepo.ts`
- Create: `src/api/supabase/notificationsRepo.ts`

**Interfaces:**
- Produces:
  - `followUser(followeeId): Promise<void>`、`unfollowUser(followeeId): Promise<void>`、`isFollowing(followeeId): Promise<boolean>`、`getFollowCounts(userId): Promise<{ followers: number; following: number }>`
  - `listNotificationsRemote(): Promise<NotificationItem[]>`、`markAllReadRemote(): Promise<void>`、`unreadCountRemote(): Promise<number>`

> 说明：网络仓库，无单测，靠 typecheck。先读 `src/api/client.ts` 确认 `supabase()`、`src/store/auth.ts` 取当前 userId 方式。

- [ ] **Step 1: 写 followsRepo**

Create `src/api/supabase/followsRepo.ts`:

```ts
import { supabase } from '@/api/client';
import { useAuth } from '@/store/auth';

function currentUserId(): string | null {
  return useAuth.getState().user?.id ?? null;
}

export async function followUser(followeeId: string): Promise<void> {
  const uid = currentUserId();
  if (!uid) throw new Error('未登录');
  const { error } = await supabase().from('follows').insert({ follower_id: uid, followee_id: followeeId });
  if (error) throw error;
}

export async function unfollowUser(followeeId: string): Promise<void> {
  const uid = currentUserId();
  if (!uid) throw new Error('未登录');
  const { error } = await supabase().from('follows')
    .delete().eq('follower_id', uid).eq('followee_id', followeeId);
  if (error) throw error;
}

export async function isFollowing(followeeId: string): Promise<boolean> {
  const uid = currentUserId();
  if (!uid) return false;
  const { data } = await supabase().from('follows')
    .select('follower_id').eq('follower_id', uid).eq('followee_id', followeeId).maybeSingle();
  return !!data;
}

export async function getFollowCounts(userId: string): Promise<{ followers: number; following: number }> {
  const [followersRes, followingRes] = await Promise.all([
    supabase().from('follows').select('*', { count: 'exact', head: true }).eq('followee_id', userId),
    supabase().from('follows').select('*', { count: 'exact', head: true }).eq('follower_id', userId),
  ]);
  return { followers: followersRes.count ?? 0, following: followingRes.count ?? 0 };
}
```

- [ ] **Step 2: 写 notificationsRepo**

Create `src/api/supabase/notificationsRepo.ts`:

```ts
import { supabase } from '@/api/client';
import { useAuth } from '@/store/auth';
import { rowToNotification, type NotificationItem } from './notificationMapper';
import type { NotificationRow } from './rows';

const SELECT = '*, actor:profiles!notifications_actor_id_fkey(*)';

export async function listNotificationsRemote(): Promise<NotificationItem[]> {
  const { data, error } = await supabase()
    .from('notifications').select(SELECT)
    .order('created_at', { ascending: false }).limit(50);
  if (error) throw error;
  return (data as NotificationRow[]).map(rowToNotification);
}

export async function markAllReadRemote(): Promise<void> {
  const uid = useAuth.getState().user?.id;
  if (!uid) return;
  await supabase().from('notifications').update({ read: true }).eq('user_id', uid).eq('read', false);
}

export async function unreadCountRemote(): Promise<number> {
  const uid = useAuth.getState().user?.id;
  if (!uid) return 0;
  const { count } = await supabase()
    .from('notifications').select('*', { count: 'exact', head: true })
    .eq('user_id', uid).eq('read', false);
  return count ?? 0;
}
```

- [ ] **Step 3: typecheck + Commit**

```bash
npm run typecheck
git add src/api/supabase/followsRepo.ts src/api/supabase/notificationsRepo.ts
git commit -m "feat(api): follows 仓库 + notifications 仓库

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 关注按钮（视频详情页作者处）

**Files:**
- Create: `src/components/social/FollowButton.tsx`
- Modify: `app/video/[id].tsx`（作者名旁加关注按钮）

**Interfaces:**
- Consumes: followsRepo（followUser/unfollowUser/isFollowing）、hasSupabase、useAuth。
- Produces: `<FollowButton targetUserId={string} />`。

> 说明：先读 video/[id].tsx 作者展示区结构。FollowButton 用 react-query：query isFollowing + mutation follow/unfollow，乐观或 invalidate。

- [ ] **Step 1: 写 FollowButton**

Create `src/components/social/FollowButton.tsx`:

```tsx
import { Pressable, Text, StyleSheet } from 'react-native';
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { followUser, unfollowUser, isFollowing } from '@/api/supabase/followsRepo';
import { hasSupabase } from '@/api/client';
import { useAuth } from '@/store/auth';
import { colors, radius, spacing, typography } from '@/theme';

export function FollowButton({ targetUserId }: { targetUserId: string }) {
  const qc = useQueryClient();
  const { user } = useAuth();
  // 自己不显示关注自己；未配 Supabase 不显示
  if (!hasSupabase || !user || user.id === targetUserId) return null;

  const { data: following = false } = useQuery({
    queryKey: ['isFollowing', targetUserId],
    queryFn: () => isFollowing(targetUserId),
  });

  const mut = useMutation({
    mutationFn: () => (following ? unfollowUser(targetUserId) : followUser(targetUserId)),
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ['isFollowing', targetUserId] });
      qc.invalidateQueries({ queryKey: ['followCounts', targetUserId] });
    },
  });

  return (
    <Pressable
      style={[styles.btn, following && styles.btnFollowing]}
      onPress={() => mut.mutate()}
      disabled={mut.isPending}
    >
      <Text style={[styles.text, following && styles.textFollowing]}>
        {following ? '已关注' : '关注'}
      </Text>
    </Pressable>
  );
}

const styles = StyleSheet.create({
  btn: { paddingHorizontal: spacing.md, paddingVertical: spacing.xs, borderRadius: radius.pill, backgroundColor: colors.primary },
  btnFollowing: { backgroundColor: 'transparent', borderWidth: 1, borderColor: colors.border },
  text: { ...typography.captionStrong, color: '#fff' },
  textFollowing: { color: colors.textMuted },
});
```

（若 theme 无 `spacing.xs` 或 `colors.border`，先 grep 确认用现有 key。）

- [ ] **Step 2: video detail 接入**

`app/video/[id].tsx`：在作者名展示处旁渲染 `<FollowButton targetUserId={video.author_id} />`（仅当 author_id 非空）。import 之。

- [ ] **Step 3: typecheck + Commit**

```bash
npm run typecheck
git add src/components/social/FollowButton.tsx "app/video/[id].tsx"
git commit -m "feat(social): 关注按钮 + 视频详情页接入

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: 个人页关注/粉丝数

**Files:**
- Modify: `app/(tabs)/profile.tsx`

**Interfaces:**
- Consumes: followsRepo.getFollowCounts、hasSupabase。

> 说明：个人页顶部现有 播放/点赞/被续写 三个 Stat（profile.tsx ~113-118）。加 关注数/粉丝数。

- [ ] **Step 1: profile 加关注/粉丝数**

`app/(tabs)/profile.tsx`：用 react-query 查 `getFollowCounts(user.id)`（queryKey `['followCounts', user.id]`，enabled: hasSupabase && !!user）。在 statsRow 加两个 Stat：「关注 {following}」「粉丝 {followers}」。非 Supabase 模式不显示这两项（或显示 0）。

```tsx
import { getFollowCounts } from '@/api/supabase/followsRepo';
// ...
  const { data: followCounts = { followers: 0, following: 0 } } = useQuery({
    queryKey: ['followCounts', user?.id],
    queryFn: () => getFollowCounts(user!.id),
    enabled: hasSupabase && !!user,
  });
```

在 statsRow 渲染处加 `<Stat label="关注" value={followCounts.following} />` 和 `<Stat label="粉丝" value={followCounts.followers} />`。

- [ ] **Step 2: typecheck + Commit**

```bash
npm run typecheck
git add "app/(tabs)/profile.tsx"
git commit -m "feat(social): 个人页显示关注/粉丝数

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: inbox 接真数据 + 标记已读 + 状态组件

**Files:**
- Modify: `app/(tabs)/inbox.tsx`

**Interfaces:**
- Consumes: notificationsRepo（listNotificationsRemote/markAllReadRemote）、NotificationItem、UserAvatar、LoadingState/ErrorState/EmptyState、hasSupabase。

> 说明：替换 mock。按 type 渲染图标 + actor UserAvatar + 文案 + 时间；进页面 markAllRead。保留非 Supabase 的空态。

- [ ] **Step 1: 重写 inbox**

`app/(tabs)/inbox.tsx`：
- hasSupabase 时用 react-query `listNotificationsRemote()`（queryKey `['notifications']`）；isLoading→LoadingState、isError→ErrorState onRetry、空→EmptyState（title「还没有通知」）。
- 渲染每条：左 type 图标（沿用现有 ICONS 映射 like/comment/fork/follow）+ actor 的 UserAvatar + 文案（`@{actorName} {动作文案}`，动作文案按 type：like→赞了你的视频 / comment→评论了你的视频 / fork→续写了你的视频 / follow→关注了你）+ 相对时间（用 createdAt）+ 未读高亮。
- 进页面（useFocusEffect 或 useEffect）调 `markAllReadRemote()` 然后 invalidate `['notifications']` 和 `['unreadCount']`。
- 非 Supabase 模式显示 EmptyState（通知是云端能力）。
- 相对时间可写个小 helper `timeAgo(ms): string`（如「刚刚/N分钟前/N小时前/N天前」）。

- [ ] **Step 2: typecheck + Commit**

```bash
npm run typecheck
git add "app/(tabs)/inbox.tsx"
git commit -m "feat(social): inbox 接真通知数据 + 进页面标记已读 + 状态组件

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8: inbox tab 未读红点

**Files:**
- Modify: `app/(tabs)/_layout.tsx`

**Interfaces:**
- Consumes: notificationsRepo.unreadCountRemote、hasSupabase。

> 说明：先读 `app/(tabs)/_layout.tsx` 看 Tab 配置（expo-router Tabs），inbox tab 加 tabBarBadge。

- [ ] **Step 1: tab 加未读红点**

`app/(tabs)/_layout.tsx`：用 react-query 查 `unreadCountRemote()`（queryKey `['unreadCount']`，enabled: hasSupabase，refetchInterval 可设 30s 或 refetchOnWindowFocus）。给 inbox 的 Tabs.Screen 设 `options={{ tabBarBadge: count > 0 ? count : undefined }}`。

- [ ] **Step 2: typecheck + Commit**

```bash
npm run typecheck
git add "app/(tabs)/_layout.tsx"
git commit -m "feat(social): inbox tab 未读红点

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 完成标准（M4-B 验收）
- 关注/取关他人，按钮状态正确，个人页关注/粉丝数变化。
- 他人点赞/评论/续写你的视频、关注你 → 你的 inbox 收到对应通知（actor、类型、时间正确）。
- 自己对自己的动作不产生通知。
- inbox 进页面后未读清零，tab 红点消失。
- 未配 Supabase 时关注按钮不显示、inbox 空态，不崩。
- `npm run typecheck` 与 `npm run test:run` 全绿。

## 人工操作
- 执行 migration 0009（social 表）+ 0010（触发器）。
- 端到端验证（建议用两个账号互相点赞/评论/续写/关注，看通知）。

## 后续（不在本计划）
- M4-C 个人资料编辑（username + 头像上传 + bio）。届时 UserAvatar/通知 actor 头像接真 avatar_url。
- inbox 逐条已读（本次进页面全标已读）。
